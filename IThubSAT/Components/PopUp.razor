<div id="popup" class="@(IsActive ? "active" : "")">
    <div  class="popup-window" style="display: @(IsActive ? "block" : "none")">
        <div class="popup-header"><div class="popup-title">@(Title)</div><div class="close-popup-button" @onclick=@OnCloseButtonClick>x</div></div>
        <div class="popup-content">@(Content)</div>
        <div class="popup-buttons">@(Buttons)</div>
    </div>
</div>

@code {
    [Parameter] public bool IsActive { get; set; }
    [Parameter] public string Title { get; set; } = string.Empty;
    [Parameter] public string Content { get; set; } = string.Empty;
    [Parameter] public RenderFragment Buttons { get; set; } = null!;
    [Parameter] public EventCallback OnCloseButtonClick { get; set; }
}